gem 'nokogiri'
  # solid xml parser, even though it requires installing more libxml and such
  # had odd encoding issues with xml-simple
  # has random parsing/search issues with hpricot

group :development do
  gem "bundler", "~> 1.0.0"
  gem "jeweler", "~> 1.6.0"
end
